export const ZKSYNC_LIST = `${window.location.origin}/data/token-list.json`

// the Uniswap Default token list lives here
export const DEFAULT_TOKEN_LIST_URL = ZKSYNC_LIST

export const DEFAULT_LIST_OF_LISTS: string[] = [
    DEFAULT_TOKEN_LIST_URL
]
